<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="http://www.springframework.org/schema/beans"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx.xsd
       http://www.springframework.org/schema/data/mongo
       http://www.springframework.org/schema/data/mongo/spring-mongo.xsd">

    <!-- mongo配置Start -->
   <!--  <mongo:mongo-client id="mongo-client" host="${mongodb.url}" port="${mongo.port}"
                        credentials="${mongo.username}:${mongo.password}@${mongodb.databaseName}">
        <mongo:client-options connections-per-host="${mongo.connections.per.host}"
                              threads-allowed-to-block-for-connection-multiplier="${mongo.threads.allowed.to.block.for.connection.multiplier}"
                              connect-timeout="${mongo.connect.timeout}"
                              socket-timeout="${mongo.socket.timeout}"
                              write-concern="${mongo.write.concern}"/>
    </mongo:mongo-client>
    <mongo:db-factory id="mongo-factory" mongo-ref="mongo-client" dbname="${mongodb.databaseName}"/>
    <bean id="mongoTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
        <constructor-arg name="mongoDbFactory" ref="mongo-factory"/>
    </bean> -->
    <!-- mongo End -->
</beans>